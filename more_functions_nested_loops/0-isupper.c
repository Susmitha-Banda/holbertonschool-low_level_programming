#include "main.h"
\**
 *check for a uppercase character
 *_isupper: check if it is uppercase
 *
 *Return: 1 if c is uppercase else 0
 *\
int _isupper(int c)
{
char ch;
if (ch == 'A' ; ch <= 'Z' ; ch++)
{
_putchar(ch);
else if (ch == c)
return (1);
else
return (0);
}
_putchar('\n');
}
