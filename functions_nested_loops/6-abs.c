#include "main.h"
/**
 * _abs - print the absolute value of an integer
 * @i: the number to be processed
 *
 * Return: the absolute value of an integer
 */
int _abs(int i)
{
if (i >= 0)
return (i);
else
return (-i);
}
